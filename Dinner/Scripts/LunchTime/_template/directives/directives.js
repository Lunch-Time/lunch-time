/**
 * directives.
 * @file directives.js.
 * @copyright Copyright ©
 */
define(
    [
        'angular'
    ],
    function(angular) {
        'use strict';

        var dependencies = [];
        var directives = angular.module('lunchtime._template.directives', dependencies);
        // directives.directive('directive', directive);

        return directives;
    });