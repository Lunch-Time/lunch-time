/**
 * controllers.
 * @file controllers.js.
 * @copyright Copyright ©
 */
define(
    [
        'angular'
    ],
    function(angular) {
        'use strict';

        var dependencies = [];
        var controllers = angular.module('lunchtime._template.controllers', dependencies);
        // controllers.controller('controller', controller);

        return controllers;
    });