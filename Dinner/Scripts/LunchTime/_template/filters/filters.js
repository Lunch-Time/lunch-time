/**
 * filters.
 * @file filters.js.
 * @copyright Copyright ©
 */
define(
    [
        'angular'
    ],
    function(angular) {
        'use strict';

        var dependencies = [];
        var filters = angular.module('lunchtime._template.filters', dependencies);
        // filters.filter('filter', filter);

        return filters;
    });