/**
 * services.
 * @file services.js.
 * @copyright Copyright ©
 */
define(
    [
        'angular'
    ],
    function(angular) {
        'use strict';

        var dependencies = [];
        var services = angular.module('lunchtime._template.services', dependencies);
        // services.service('service', service);

        return services;
    });